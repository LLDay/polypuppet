---
version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "Specific node data"
    path: "nodes/%{trusted.certname}.yaml"

  - name: "Students group data"
    path: "groups/%{facts.polypuppet.student_flow}/%{facts.polypuppet.student_group}.yaml"

  - name: "Students flow data"
    path: "groups/%{facts.polypuppet.student_flow}.yaml"

  - name: "Audience data"
    path: "audience/${facts.polypuppet.audience}"

  - name: "Common data"
    path: "common.yaml"
